INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/lib)

SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

AUX_SOURCE_DIRECTORY(./ DIR_SRCS)

ADD_EXECUTABLE(feed ${DIR_SRCS})

TARGET_LINK_LIBRARIES(feed lib -lwiringPi)
